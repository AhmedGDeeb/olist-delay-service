"""Placeholder — feature engineering will go here later."""

import pandas as pd


def create_label(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for label creation. Returns input unchanged for now."""
    return df


def create_features(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for feature engineering. Returns input unchanged for now."""
    return df

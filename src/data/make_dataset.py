"""Placeholder — data loading and cleaning will go here later."""

import pandas as pd


def load_data(path: str) -> pd.DataFrame:
    """Load a CSV file into a DataFrame. Placeholder for now."""
    return pd.read_csv(path)


def clean_data(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder for cleaning logic. Returns input unchanged for now."""
    return df
